# Copy this file to secrets.toml (same folder) and fill in your real key.
# secrets.toml is gitignored — never commit your actual API key.
#
# On Streamlit Community Cloud, you don't need this file at all: set the
# same key/value pair via your app's "Secrets" settings in the dashboard.
#
# Get a free Gemini API key (no card required) at https://aistudio.google.com/apikey

GEMINI_API_KEY = "your-gemini-key-here"
